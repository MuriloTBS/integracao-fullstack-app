{
  "name": "integracao-fullstack-app",
  "version": "1.0.0",
  "description": "App de exemplo para o pipeline VS Code → Claude → n8n → Render",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "build": "echo 'Build OK (adicione seu bundler aqui)'",
    "dev": "node --watch src/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
